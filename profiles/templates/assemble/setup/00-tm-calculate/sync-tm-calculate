# Calculate pkg(app-portage/layman)!= path=/tmp name=sync-tm-calculate exec=/bin/bash comment=#
#
# Sync our custom overlay (NEED FIX)

layman -s tm-calculate 2>&1 >/dev/null && eix-update 2>&1 >/dev/null

